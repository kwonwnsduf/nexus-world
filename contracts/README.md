# Contracts

`openapi/` contains synchronous service specifications, `events/` contains versioned event schemas, and `schemas/` contains shared non-event JSON Schemas. The v1 surface includes platform health, authentication, provenance, and the executable economic-civilization ontology/world graph. Run `corepack pnpm contracts:check` to validate every checked-in JSON contract.
