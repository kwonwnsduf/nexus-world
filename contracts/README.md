# Contracts

`openapi/` contains synchronous service specifications, `events/` contains versioned event schemas, and `schemas/` contains shared non-event JSON Schemas. Day 2 adds the first `v1` vertical contract from the web BFF through Core API to AI Service. Run `corepack pnpm contracts:check` to validate every checked-in JSON contract.
